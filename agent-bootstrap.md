# Clawbooks Agent Bootstrap

Run `clawbooks quickstart` to get started.
It will point you to `program.md` and `policy.md` — read both before doing real work.
Treat the ledger as append-only. The agent does the accounting; the CLI is data storage.
